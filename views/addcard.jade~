#head
    h1= title
- if ('undefined' !== typeof errors && errors.length)
  ul#errors
    - each error in errors
      li.error= error
form(action='/addcard', method='post')
  #title
    label(for=cardTitle) Card Title
    input(type='text', name=cardTitle, value='')
  #submit
    input(type='submit') Login
